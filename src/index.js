import React, {
    Component
} from 'react';
import ReactDOM from 'react-dom';

import App from 'App/app';

ReactDom.render(
    <div>
        <App />
    </div>,
    document.getElementById('root')
)